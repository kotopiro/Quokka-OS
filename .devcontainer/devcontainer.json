{
  "name": "Quokka OS build environment (Debian bookworm)",
  "image": "mcr.microsoft.com/devcontainers/base:bookworm",
  "hostRequirements": {
    "cpus": 2,
    "memory": "8gb",
    "storage": "32gb"
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y live-build debootstrap xorriso squashfs-tools isolinux syslinux-common gnupg2 wget",
  "remoteUser": "vscode"
}
